---
# yaml-language-server: $schema=https://kubernetes-schemas.falhalla.com/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: home-assistant
  namespace: default
spec:
  values:
    addons:
      codeserver:
        enabled: true
        image:
          repository: ghcr.io/coder/code-server
          tag: 4.10.1
        env:
          TZ: America/Chicago
        workingDir: /config
        args:
          - --auth
          - "none"
          - --user-data-dir
          - "/config/.vscode"
          - --extensions-dir
          - "/config/.vscode"
        ingress:
          enabled: true
          ingressClassName: nginx
          annotations:
            hajimari.io/appName: "Hass Config"
            hajimari.io/icon: cib:visual-studio-code
            hajimari.io/group: Intranet Apps
          hosts:
            - host: &host hass-code.intra.falhalla.com
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - hosts:
                - *host
        volumeMounts:
          - name: config
            mountPath: /config
      nodered:
        enabled: true
        image:
          repository: ghcr.io/hassio-addons/nodered/amd64:fd376cb
        env:
          TZ: America/Chicago
        workingDir: /opt
        args:
          - --auth
          - "none"
          # - --user-data-dir
          # - "/config/.vscode"
          # - --extensions-dir
          # - "/config/.vscode"
        ingress:
          enabled: true
          ingressClassName: nginx
          annotations:
            hajimari.io/appName: "Node Red"
            hajimari.io/icon: mdi:atom-variant
            hajimari.io/group: Intranet Apps
          hosts:
            - host: &host nodered.intra.falhalla.com
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - hosts:
                - *host
        volumeMounts:
          - name: config
            mountPath: /config