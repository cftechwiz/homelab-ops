apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: ceph-multus-net
spec:
  config: '{
    "cniVersion": "0.3.1",
    "type": "macvlan",
    "master": "eno7",
    "mode": "bridge",
    "ipam": {
    "type": "calico",
    "range": "172.16.70.0/24"
    }
    }'
