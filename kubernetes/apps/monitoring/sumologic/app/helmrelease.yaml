---
# yaml-language-server: $schema=https://kubernetes-schemas.falhalla.com/helmrelease_v2beta1.json
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sumologic
  namespace: monitoring
spec:
  interval: 15m
  chart:
    spec:
      chart: sumologic
      version: 3.1.0
      sourceRef:
        kind: HelmRepository
        name: sumologic
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  envFromSecrets:
    - name: sumologic-secret
  # valuesFrom:
  #   - targetPath: sumologic.accessId
  #     kind: Secret
  #     name: values-yaml
  #     valesKey: SUMO_ACCESS_ID
  #   - targetPath: sumologic.accessKey
  #     kind: Secret
  #     name: values-yaml
  #     valesKey: SUMO_ACCESS_KEY
  #   - targetPath: sumologic.clusterName
  #     kind: Secret
  #     name: values-yaml
  #     valesKey: SUMO_CLUSTER_NAME
  #         - Values.sumologic.accessId: ${SUMO_ACCESS_ID}
  #         - Values.sumologic.accessKey: ${SUMO_ACCESS_KEY}
  #         - Values.sumologic.clusterName: ${SUMO_CLUSTER_NAME}
