# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: cluster-secrets
    namespace: flux-system
stringData:
    SECRET_ALERTMANAGER_PUSHOVER_TOKEN:
    SECRET_ALERTMANAGER_PUSHOVER_USER_KEY:
    SECRET_ALERTMANAGER_UPTIMEROBOT_HEARTBEAT_TOKEN:

